import React from "react";
import { View, Text } from "react-native";
import { Layout } from "../templates/Layout";

export const Notify = () => {
  return (
    <Layout>
      <View></View>
    </Layout>
  );
};
